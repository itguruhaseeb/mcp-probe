# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# Build / coverage artifacts
coverage/
dist/
*.tgz

# Census raw frame (8MB+, regenerable via `node benchmark/harvest.mjs`;
# the versioned copy lives in the Zenodo dataset release, not git).
benchmark/corpus.json
